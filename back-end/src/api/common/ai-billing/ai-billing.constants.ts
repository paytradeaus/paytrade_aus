/** Task #161 — Admin-only setting controlling user-charged cost vs raw OpenAI cost. */
export const AI_USER_COST_MULTIPLIER_SETTING_NAME = 'ai_user_cost_multiplier';
export const AI_USER_COST_MULTIPLIER_DEFAULT = 2.0;

/** Default plan AI credit values used by the seeder when backfilling existing plans. */
export const PLAN_DEFAULT_MONTHLY_AI_CREDIT = 0;

/** Admin menu IDs (UUID v5-style namespaced under the existing admin-menu seeder). */
export const ADMIN_MENU_AI_PURCHASES_ID =
  'a0000001-0000-0000-0000-000000000023';
