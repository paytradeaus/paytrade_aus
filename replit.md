# PayTrade Application

## Overview
PayTrade is a full-stack application designed to streamline payment, invoicing, contract management, and trust accounting specifically for the construction industry. It provides a comprehensive solution for managing financial workflows, ensuring compliance, and leveraging modern web technologies for a robust and scalable platform. The project aims to be a leading financial management solution in the construction sector, enhancing efficiency and reducing administrative burden for businesses.

## User Preferences
I prefer clear, concise communication. When making changes, prioritize iterative development and explain the high-level approach before diving into specifics. Please ask for confirmation before implementing significant architectural changes or altering core business logic.

## System Architecture
The application features a Next.js frontend and a NestJS backend communicating via a GraphQL API.

**Frontend (Next.js)**
- **Framework**: Next.js 14.2.11 with React 18.
- **State Management**: Redux Toolkit.
- **API Client**: Apollo Client for GraphQL.
- **Styling**: SCSS Modules and standard CSS.
- **UI/UX**: Dynamic pricing tables, plan cards with tier-specific styling, and SEO driven by backend-managed entities. The frontend is built for deployment stability across multiple environments (Dev, Staging, Production).

**Backend (NestJS)**
- **Framework**: NestJS.
- **Database**: PostgreSQL with TypeORM.
- **Queue**: BullMQ with Redis for asynchronous jobs.
- **Authentication**: JWT with Passport.
- **File Storage**: Primary storage on Cloudflare R2 using `@aws-sdk/client-s3`, with a read-fallback to Replit Object Storage for legacy files. Supports direct file serving.
- **Logging**: Custom `PaytradeLogger` for local files and Cloudflare R2 in production.
- **Email Sending Patterns** (two intentional patterns; pick by use case):
  - **DB-template-driven** — `emailTemplateRepo.findOne({ email_type: '<key>' })` → fill placeholders → `emailQueueProducer.emailQueueProducer(...)`. Used for **admin-editable, reusable templates** where the body must be editable from the Communication Management UI without a code deploy: signup/verification/password-reset, notices and `notice-gen-doc`, support replies, contact onboarding, subscription invoices, community digest, generic integration emails. New templates of this kind MUST ship with an idempotent `email_templates` seeder (mirror the `notice-templates-seeder` pattern, key on the unique `email_type` column, do not overwrite existing rows so admin edits are preserved).
  - **Inline `header-footer-email` wrapper** — body composed in code, the Handlebars `header-footer-email` template provides the chrome; no `email_templates` row is read or required. Used for **system-generated, event-driven notifications** whose content is fully computed and not meaningfully editable by an admin: admin-composed ad-hoc emails, admin/staff lifecycle (`pt-admin-access`, `pt-admin`), subscription state changes, community moderation, integration status changes (e.g. Xero "connection inactive" notification — `xero-scheduler.service.ts` `notifyPrimaryAdminOfXeroInactive`). New notifications of this kind do NOT need a seeder; they only need the existing `header-footer-email` template (already shipped) and a sensible `EmailTypeEnum` value for the `mail_type` log tag.
- **PDF Generation**: Utilizes Puppeteer.
- **Community Bot**: Cron-scheduled OpenAI GPT-4o bot for Q&A content generation.
- **Monitoring**: `/health` endpoint for database checks, `production-server.js` for backend/frontend health monitoring with email alerts, and auto-restart mechanisms.
- **Database Backups**: Automated `pg_dump | gzip` backups to Cloudflare R2 every 6 hours in production, with tiered retention policies.
- **Startup Seeders**: `OnApplicationBootstrap` seeders for admin menus, compliance data, and Xero log templates, designed to insert new data without overwriting existing records.
- **Admin User Management**: Comprehensive CRUD for admin users, groups, and permissions, including tools for menu editing, pricing table management, holiday monitoring, company deletion with impact assessment, and user import/export functionality.
- **SEO & How-To Guides Systems**: Backend-managed systems for dynamic landing pages with SEO keywords and categorized how-to guides.
- **AI Support Assistant**: A search-first support flow incorporating OpenAI GPT-4o for answering user queries, with features like rate limiting, relevance gating, and web search enrichment. A reusable `AiHelpWidget` provides contextual AI support.
- **Smart Reconciliation Matching**: QuickBooks-style smart matching system for bank transactions, supporting exact and near matches with configurable tolerance and quick-adjust functionalities, all within atomic transactions.
- **Xero Contact GST Sync (Phase 2)**: Two-way per-contact GST defaults between PT and Xero. New columns `client_suppliers_details.xero_sales_gst_setting` / `xero_purchases_gst_setting` (text, NULL = use org default), `company_details.is_gst_registered` (nullable bool, NULL = unknown), and `xero_integration_details.xero_org_country_code / xero_org_is_gst_registered / xero_org_sales_tax_basis / xero_org_default_sales_tax / xero_org_default_purchases_tax / xero_org_settings_synced_at` (cache of Xero org GST defaults). Idempotent `XeroContactGstSchemaSeederModule` adds the columns in production via `ALTER TABLE IF NOT EXISTS`.
  - **Outbound (PT → Xero)**: All four contact-write paths in `xero-contacts.service.ts` (createContact ~L286, insertContactDetailsInPaytrade post-import update ~L765, editContact ~L1085, batch create ~L297) call `applyContactGstToXeroPayload(target, ptContact)` to copy `xero_sales_gst_setting` → `salesDefaultTaxType` and `xero_purchases_gst_setting` → `purchasesDefaultTaxType`. The sentinel value "Use organisation settings" and blank/null are skipped, leaving the Xero default untouched.
  - **Inbound (Xero → PT)**: `webhook.service.ts` `handleContactCreateUpdate` ~L348 calls `persistContactGstFromXero(pt_contact_id, contact)` after every contact webhook. The batch import paths (syncAll/refreshContacts/autoCreateSingle) intentionally rely on the per-contact webhook fan-out plus the manual `backfillContactGstFromXero` mutation for one-time backfill, since `summaryOnly=true` Xero list calls strip the tax defaults.
  - **Org defaults cache**: `XeroService.refreshOrgGstDefaults(company_id)` calls `getOrganisations`, derives `isGstRegistered` from `salesTaxBasis !== 'NONE'`, and writes the cache. Wired into the existing hourly `XeroSchedulerService.checkAndRunJobs` cron after `refreshTokenSet`, best-effort (errors are logged, never blocking).
  - **`resolveContactGstStatus(contact, claimType, orgDefaults?, company?)` helper** (`back-end/src/api/common/integrations/xero/contacts/contact-gst-resolver.ts`) — pure function returning `{ taxType, source, resolved }`. Resolution order: per-contact override → cached Xero org default → `company.is_gst_registered` ('GST on Income/Expenses' if true, 'BAS Excluded' if false) → `{ taxType: null, source: 'unknown', resolved: false }`. Sales vs purchases path is keyed off `claimType === 'Receivable'`.
  - **Alignment endpoints**: `backfillContactGstFromXero` and `backfillContactGstFromPaytrade` GraphQL mutations in `xero-contacts.resolver.ts` iterate over every mapped contact for the calling company and either pull or push the per-contact GST defaults; both return a JSON-stringified `{ scanned, updated|pushed, skipped, errors }` summary suitable for displaying in the Xero Settings widget.
  - **Frontend**: `AddClientsAndSuppliers` form gains two optional dropdowns (sales/purchases GST tax type) sourced from `xeroGstTypeOptions` constant; "Use organisation settings (default)" maps to NULL. `BusinessProfile` adds an `is_gst_registered` checkbox under "Manage email preferences"; the value flows through `BusinessProfileContext` → `updateCompanyDetails` mutation. `UpdateCompanySignupInput` accepts `is_gst_registered`; `signup.service.ts` `updateCompanyDetails` ~L1124 persists it (only when explicitly provided, preserving NULL = unknown).
- **Xero Retention Auto Gross-Up Journals (Phase 3)**: When `xero_integration_details.auto_gross_up_retention_journals=true` AND `simplified_retention_accounting=false` AND `retention_recording_mode='ex_gst'`, PayTrade posts a balanced 2-line POSTED Manual Journal in Xero for the GST portion of retention so the held/release ledgers reconcile to the gross retention figure. New entity `XeroRetentionJournals` (`xero_retention_journals` table, unique partial index on `manual_journal_id IS NOT NULL` for anti-echo). New service `XeroManualJournalService` exposes `isAutoGrossUpEnabled / resolveGstApplicability / postGrossUpJournal / voidGrossUpJournal / voidAllForClaim / findByManualJournalId / listJournalsForClaim`. **Smart tax-type resolution order**: (1) bill/invoice base line `taxType` → (2) Phase 2 `resolveContactGstStatus` helper (per-contact override → cached Xero org default → `company.is_gst_registered`) → (3) `unknown` (no MJ posted, sync log written). `kind='gross_up'` on Claim creation (DR Retention Payable / CR Retention Held = GST), `kind='gross_up_reversal'` on `cash_retention_type='Retention claim'` (opposite signs). **Wire points**: outbound `xero-invoices.service.ts` `createInvoiceOrBillInXero` (post create) / `editInvoicesOrBills` (void+recreate) / `deleteInvoicesOrBills` (`voidAllForClaim`); inbound `webhook.service.ts` `createClaimInPaytrade` post-success. All call sites are wrapped in try/catch — failures write a sync log and never block the underlying claim flow. **Anti-echo**: `webhook-queue-consumer.service.ts` dispatches `MANUALJOURNAL.CREATE/UPDATE` to `webhook.service.ts.handleManualJournalUpdate` which checks `findByManualJournalId` and drops self-echoes (sync log template 606). External MJs are no-op for now. **Sync log templates**: 600 (gross_up posted) / 601 (skipped) / 602 (failed) / 603 (reversal posted) / 604 (voided) / 605 (release reversal) / 606 (anti-echo). **Frontend**: `XeroIntegration.tsx` claim drawer expander adds a "Retention GST gross-up journals" table fed by GraphQL `getXeroRetentionJournalsForClaim` (newest first, shows kind/status/ex-GST/GST/tax-type/source/posted timestamp + error text on FAILED rows). Idempotent `XeroRetentionJournalsSchemaSeederModule` adds the column + table in production via `ALTER TABLE IF NOT EXISTS`/`CREATE TABLE IF NOT EXISTS`.
- **Xero Retention Recording Mode (Phase 1)**: `xero_integration_details` carries `retention_recording_mode` enum (`ex_gst` default / `inc_gst`) and optional `retention_tax_type` text. `getRetentionLineSpec()` accepts both — when `inc_gst` AND the invoice is `Inclusive`, retention/liability/release lines are gross-upped (`* 1.1`) regardless of the destination Xero account's tax type, and `retention_tax_type` overrides the account-derived `taxType` on the line. The consumer split (xero-invoices.service.ts ~L2597 and webhook V-Step ~L2925 / D-Step ~L3730) honours the same mode: when `inc_gst` + Inclusive, `retentionUnitOnly` is derived from `unitAmount/1.1` and `retentionTaxOnly` from `unitAmount - unitAmount/1.1` instead of trusting the (often zero) per-line `taxAmount`. `adjustItemsWithRetention()` now recomputes per-line GST as `newUnitAmount × perLineRate` (rate = `item.taxAmount / unitAmount`, fallback to `invoice.totalTax / invoice.subTotal`, then `0.10`) so the restored bill_code line's GST always equals `unit_price × rate` (fixes the $1,493.78 vs $1,572.40 mismatch on a $15,724 unit price). The Add/Edit Claim form's `Unit price` header is labelled `Unit price (ex-GST)` with a tooltip clarifying the GST tickbox adds 10% on top.
- **Xero Retention/GST Handling (Producer)**: When PT pushes claims to Xero, retention and liability lines are constructed via `getRetentionLineSpec()` which sets an explicit `taxType` per line (looked up from the Xero account, 5-min cached) and only grosses up `retention_amount` to inc-GST when (a) the invoice's `lineAmountTypes` is Inclusive AND (b) the destination account's tax type is GST-applicable (excludes BASEXCLUDED/NONE/EXEMPT*/INPUTTAXED). For Exclusive invoices the line stays ex-GST. For NoTax invoices no taxType is set. This prevents over-grossing on BAS-Excluded retention accounts. `retention_amount` is stored ex-GST in PT and the share deducted from each bill_code line uses the ex-GST value (no `* 1.1`). Base bill/invoice line `unitAmount`, `totalLineAmount`, and retention share proration all key off the resolved `lineAmountTypes` (`isInclusiveLine`) rather than `claimDetails.is_gst_optional`, so Exclusive updates send ex-GST `unit_price` (preventing Xero from double-applying GST when the original invoice was Exclusive).
- **Xero Token Refresh Lock**: Redis-based per-company mutex (`xero-token-lock:{company_id}`) in `XeroService.refreshTokenSet()` prevents concurrent OAuth2 token refreshes from invalidating each other. Uses `SET NX EX` for acquire, Lua compare-and-delete for safe release, with 15s TTL and 10s wait. Waiters re-read the DB for freshly refreshed tokens.
- **Xero Integrations (Auto-Creation & Sync)**:
    - **Bank Account Auto-Create**: Two-way synchronization between PayTrade and Xero for bank accounts, with hourly schedulers and manual sync options.
    - **Contact Auto-Create**: Two-way synchronization for contacts, supporting individual and batch creation with type determination (Client/Supplier) from Xero data.
    - **Smart Contract Auto-Creation**: Automatically creates contracts from Xero claims (invoices/bills) based on predefined logic and defaults when no existing contract matches. Validation consolidates all missing fields (Address, Email, bank details, PTA/RTA accounts) into a single error message.
    - **Project & Contract Auto-Create**: Two-way synchronization for projects and contracts, mapping them to Xero tracking categories, with individual and batch creation options.
    - **Manual Contact Financial Sync**: On-demand synchronization of financial details for mapped contacts.
    - **Manual Contact Information Sync**: On-demand synchronization of address, phone, and email from Xero to PayTrade for all mapped contacts, with a dedicated "SYNC CONTACT INFO" button alongside "SYNC FINANCIAL DETAILS".
    - **Webhook Contact Detail Sync**: Real-time sync of address (PO Box/Street), phone (Mobile/Default), and email from Xero when contacts are updated, in addition to name and email.
    - **Sync Log Summary Dashboard Widget**: Displays a summary of recent Xero sync activity on the user dashboard, with colour-coded status breakdown (Synced/Warnings/Failed) and the 10 most recent sync log entries.
    - **Webhook Fallback Scheduler**: Cron-scheduled (every 15 min) fallback that polls Xero API for recently modified invoices and contacts, compares against tracked records in `xero_invoices_bills` and `xero_contact_details`, and processes any gaps missed by webhooks. Uses `sync_run_type: 'fallback'` to distinguish from webhook-triggered syncs.
- **Demo/Sandbox Mode**: Per-company demo mode with isolated Stripe test keys for payments, allowing companies and subscription plans to operate in a sandbox environment.
- **Xero Invoice/Bill on Claims + Audit Pack**: `xero_invoices_bills` carries cached_pdf_object_key, last_fetched_at, deep_link_url, current_xero_status, is_stale, void_date. PDFs are pulled via `getInvoiceAsPdf` and stored on R2 under `xero_pdfs/<invoiceId>.pdf`. The webhook handler (INVOICE.UPDATE/CREATE) refreshes the cache; DELETE/VOID flips `is_stale=true` while preserving the cached PDF for audit. The 15-min webhook fallback scheduler reuses the same handler so cache refresh is automatic. `XeroSchedulerService.onApplicationBootstrap()` runs a throttled (~55/min) backfill so legacy rows get a cached PDF. Frontend `XeroIntegration` expander in `AddUpdateClaims.tsx` shows invoice number, status, deep link and a Download Xero PDF button — the latter calls GraphQL `getXeroInvoicePdfDownloadToken` (5-min JWT) and then GET `/files/xeroPdf` with `Authorization: Bearer <token>`. Audit-pack ZIPs include the cached PDF in `<module>/Attachments/Xero-<reference|invoiceId>.pdf` and the per-claim Excel adds Xero Invoice #, Xero Status, Xero Link columns.
- **Xero Payment/Retention Sync Gate Split (Task #50)**: Each confirmation checkbox now fires its own Xero leg independently — Confirm Paid → `Payment` API; Confirm Retention → `BankTransfer` API. Previously both had to be ticked before any push fired. New `xero_payments.bank_transfer_reference` column caches the `PT-RET-{pt_payment_id}` reference stamped on outbound BankTransfers; new unique partial index `uq_xero_payments_bank_transfer_id` (where `bank_transfer_id IS NOT NULL`) hard-guarantees one PT row per Xero BankTransfer. Idempotent `XeroPaymentSplitSchemaSeederModule` adds both in production via `ALTER TABLE IF NOT EXISTS` / `CREATE UNIQUE INDEX IF NOT EXISTS`.
  - **Outbound (`xero-payments.service.ts createPayment`)**: Accepts `sync_payment` and `sync_transfer` flags via `CreatePaymentInput` (defaults true). At call time, looks up any existing `xero_payments` row for `pt_payment_id + integration_id` and computes `skipPayment = !wantPayment || existingXp.payment_id` and `skipTransfer = !wantTransfer || existingXp.bank_transfer_id`. If both skipped, returns the existing row without hitting Xero. Otherwise pushes only the missing leg(s) and **updates** the existing row instead of inserting a duplicate. The BankTransfer is stamped with `reference: 'PT-RET-{pt_payment_id}'` for the inbound matcher's round-trip shortcut. The cached reference is persisted to `bank_transfer_reference`.
  - **Resolver gate split (`payments.resolver.ts addDetailsOfAPayment` ~L206 + `editDetailsOfAPayment` ~L840)**: Both call sites now compute `wantPayment = isPaymentChecked && !paymentLegSynced` and `wantTransfer = cash_retention && isRetentionChecked && !transferLegSynced` and fire `createPayment` whenever either is true. The legacy `(isPaymentChecked && isRetentionChecked)` AND-gate is removed.
  - **Inbound matcher (`webhook.service.ts` ~L9229)**: Replaces the legacy "OR on accounts + amount" filter with a four-step pipeline:
    1. **Reference round-trip** — prefers transfers whose `reference` equals `PT-RET-{pt_payment_id}` of the existing/previous PT payment (covers webhook echo of an outbound transfer we just pushed). Logged as template **488 — Succeeded**.
    2. **AND on accounts + ±14 day window** — at least one transfer side must equal the payment account AND the *other* side must be a different account; transfer date must fall within ±14 days of the Xero payment date.
    3. **Out-of-window rejection** — same-amount, same-account candidates outside the window are surfaced as template **489 — Failed** (`RETENTION_TRANSFER_OUT_OF_WINDOW`) so users see why nothing matched.
    4. **Multi-match after tightening** — if more than one transfer survives, all candidates are logged as template **490 — Failed** (`RETENTION_TRANSFER_MULTIPLE_MATCH`) and the inbound import returns false. Existing per-row uniqueness check (`checkBankTransferIdExistence`) and the new unique partial index together guarantee a transfer cannot be double-mapped.
  - **ACCREC parity (`webhook.service.ts` ~L10895)**: `is_retention_confirmed` no longer requires `invoice.type === ACCPAY`. Inbound ACCREC invoices with cash retention also auto-confirm the retention leg, mirroring the outbound gate split that now pushes BankTransfers for both bills and invoices.
  - **Sync log templates 488 / 489 / 490** added at the tail of `xero-log-templates.json` (the `XeroLogTemplatesSeeder` is idempotent — it inserts new ids without overwriting admin edits).
  - **Symmetric un-tick → delete in Xero (Task #52)**: Un-ticking a previously-synced confirmation now deletes the matching Xero record so PT and Xero stay in sync. `DeletePaymentInput` carries optional `delete_payment` / `delete_transfer` flags (default true → legacy combined behavior preserved for the existing "Unconfirmed - Unmatched" call site). When either flag is explicitly false, `xero-payments.service.ts` `deletePayment` routes to the new private `deletePaymentPerLeg` helper which fires only the requested leg(s): Payment delete sets `status: DELETED` via `accountingApi.deletePayment` and clears `xero_payments.payment_id` (sync log template **491**); BankTransfer reversal posts a new opposite-direction transfer (retention → operating, swapping `from`/`to`), updates `xero_payments.bank_transfer_id` to the reversal id, clears `bank_transfer_reference` (sync log template **492**). `payments.resolver.ts editDetailsOfAPayment` ~L862 detects the un-tick conditions (`isPaymentChecked === false && paymentLegSynced` and the retention equivalent) and calls `deletePayment` with the matching flags. Per-leg routing in `xero-payments.service.ts deletePayment` triggers whenever either flag is **explicitly provided** (true OR false) so both-leg un-tick (both flags === true) still flows through `deletePaymentPerLeg` and emits the 491/492 sync log entries; the legacy "Unconfirmed - Unmatched" call site omits the flags entirely → both undefined → original combined behaviour. **Per-leg result tracking & partial-failure handling**: `deletePaymentPerLeg` runs each leg inside its own try/catch, accumulates outcomes in `legResults = { paymentRequested, paymentDeleted, transferRequested, transferReversed, errors[] }`, and in dual-leg mode pre-validates the transfer leg's bank-account mappings BEFORE the irreversible `accountingApi.deletePayment` call. If any requested leg failed it throws `new Error(joined)` with `legResults` attached; the resolver catch reads `legResults` and only reverts the un-tick(s) whose Xero side did NOT persist via `paymentsService.revertSubPaymentConfirmation(payment_id, { paid?, retention? })`. This prevents a payment-leg success + transfer-leg failure from re-ticking is_paid_confirmed (the Xero Payment is already deleted) while still re-ticking is_retention_confirmed (the BankTransfer is still live). Inner string throws were converted to `new Error(errMsg)` and the resolver outer catch coerces `error?.message ?? String(error)` so the GraphQL response always carries an actionable string (e.g. "Payment is already reconciled"). **Frontend**: `fetchDetailsOfAPayment` GraphQL data now exposes `xero_payment_synced` / `xero_transfer_synced` booleans (computed via raw SQL against `xero_payments` joined to the active `xero_integration_details` row). `AddPaymentSection.tsx` `onConfirmPaidChange` / `onConfirmRTAPaidChange` show the delete-warning modal only when the checkbox was originally checked **and** the corresponding Xero record is mapped (`patchData?.is_*_confirmed === true && patchData?.xero_*_synced === true`); legacy payments confirmed before Xero was connected un-tick silently. Cancel → checkbox stays ticked.
  - **Deferred to follow-up**: reconcile-on-conflict (Xero "exceeds outstanding" recovery), retro re-check scheduler for legacy unmatched payments, refusal-to-untick-once-synced UI guard, moving `retention_details` row creation from "on payment save" to "on confirmation", and the Xero IDs/UI footer tweaks. None of these block the gate-split or matcher tightening landing.
- **Manual Xero Re-sync by ID (Task #65)**: Admin-only recovery tool that re-pulls a single Xero record and re-runs the existing inbound webhook handler stamped with `sync_run_type: 'manual'`. New service method `XeroWebhookService.manualXeroResync(decoded, { company_id, type, id })` (~L13262 in `webhook.service.ts`) handles five record types: `invoice_bill` (resolves invoice number → GUID via `getInvoices` with `where: 'InvoiceNumber=="..."'` when input isn't a UUID, then calls `handleInvoiceCreateUpdate`), `payment` (resolves Xero payment → invoice id → `handleInvoiceCreateUpdate`), `bank_transfer` (parses `PT-RET-{id}` reference → linked PT payment → claim invoice → `handleInvoiceCreateUpdate`), `contact` (`handleContactCreateUpdate`), `manual_journal` (`handleManualJournalUpdate`, anti-echo aware via `xeroManualJournalService.findByManualJournalId`). Always writes a template-499 trigger log (`MANUAL_XERO_SYNC_TRIGGERED`) regardless of outcome. Reuses the existing `XeroService.refreshTokenSet` Redis-locked token refresh and the existing `xero` `XeroClient` from the constructor. **Resolver**: `manualXeroResync(company_id, type, id)` mutation in `xero.resolver.ts` gated by `@UseGuards(JwtAuthGuard, RolesGuard) @Roles(Role.ADMIN, Role.PRIMARY_ADMIN)`; non-admin standard users get a guard rejection. Returns standard `StringResponse` with JSON-stringified `{ success, message, syncLogId, resolvedXeroId }` payload, plus the standard `XERO_REFRESH` flow when the refresh token is dead. **Frontend**: `manualXeroResync` helper in `integration.functions.ts` parses the JSON payload and toasts result. New `ManualXeroSyncCard` component appended to `XeroDashboard/index.tsx` (under the cards grid, before Sync Log) — type dropdown + ID input + Run sync button + inline result panel with sync log link. Out of scope (deferred): bulk paste, fuzzy search, bulk re-pull, reverse direction (PT → Xero push), cross-company.
- **UI Status Lookup (NULL vs empty string)**: `ui_status_and_action_buttons` rows store `claim_type`/`payment_type`/`current_status` as SQL `NULL` for "not applicable" states (e.g. a Xero-imported claim that has no payments yet has `payment_type=NULL`). All four `whereConditions` builders in `back-end/src/api/users/banking/ui-status.service.ts` (lines ~205, ~459, ~740, ~1102) coerce empty-string/null inputs to TypeORM's `IsNull()` so the emitted SQL is `IS NULL` rather than `= ''`. Without this coercion, lookups for Xero-imported `Confirmed` claims with no payments missed the matching seed row and persisted `claim_overview_buttons={}`, causing the claim drawer footer to render only Close (no Edit, no Add Payment).

## External Dependencies
- **PostgreSQL**: Primary application database.
- **Redis**: Shared across all environments (dev/staging/production). Used for BullMQ job queuing and Xero webhook event queue. The webhook queue is namespaced per environment (`xero_webhook_queue:{environment}`) — each backend consumes only its own queue. Auto-detection: `REPL_ID` without `REPLIT_DEPLOYMENT` → `development`, `REPL_ID` with `REPLIT_DEPLOYMENT` → `staging`, neither → `production`. Override with `APP_ENVIRONMENT` env var. The Cloudflare webhook relay worker fans out to all three queues (`xero_webhook_queue:production`, `xero_webhook_queue:staging`, `xero_webhook_queue:development`).
- **Cloudflare R2**: Cloud storage for application files, logs, and database backups.
- **OpenAI API**: For AI content generation and support assistant.
- **Puppeteer**: For PDF document generation.
- **Stripe**: For payment processing.
- **Brevo (formerly Sendinblue)**: For email services.
- **Google Tag Manager, Google Analytics, Cookiebot**: For analytics and cookie consent management.