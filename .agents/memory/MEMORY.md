- [Email preference gating convention](email-preferences-gating.md) — opt-IN by default: gating queries treat NULL/missing as on, only explicit `false` opts out; new keys need allowlist + default + backfill.
- [XeroService cross-module providers](xero-service-cross-module.md) — XeroService is registered in 6 different modules; new dependencies must be added to ALL of them or DI fails at boot.
- [Locally-provided shared services need @Optional + standalone DI module](shared-service-optional-di.md) — when CompliancesService/XeroService is provided locally in sibling modules to avoid a circular import, adding a new injected dep must use `@Optional()` AND a tiny standalone module exporting the dep that each sibling module imports — otherwise either boot fails or the dep silently no-ops in those siblings.
