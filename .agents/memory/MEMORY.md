- [Email preference gating convention](email-preferences-gating.md) — opt-IN by default: gating queries treat NULL/missing as on, only explicit `false` opts out; new keys need allowlist + default + backfill.
- [XeroService cross-module providers](xero-service-cross-module.md) — XeroService is registered in 6 different modules; new dependencies must be added to ALL of them or DI fails at boot.
- [Shared-service DI needs @Optional + standalone module](shared-service-optional-di.md) — adding a dep to a service that's locally provided in many sibling modules needs `@Optional()` AND a tiny standalone module those siblings import.
- [pt_to_xero_contract_auto_create gating](xero-pt-auto-create.md) — per-company opt-in to auto-push new PT contracts to Xero; must be checked at EVERY contract creation site (user resolver AND smart-create), not just one.
- [Xero "Export successful" must require a real Xero ID](xero-success-requires-real-id.md) — gating success on `options[0] !== null` is not enough; Xero can return ID-less elements, producing phantom mirror rows that fail on later sync. Always check the ID field directly.
- [Xero sync logs must capture the response, not the request](xero-sync-log-capture-response.md) — `xero_records: [requestPayload]` on success logs makes the silent-success class of bug undiagnosable; always store `response.body.<entities>`.
- [Stripe subscription GST mode](stripe-subscription-gst-mode.md) — Stripe Tax stays OFF; manual TaxRates only. Signature is grandfathered inclusive via `is_gst_inclusive` flag + `LEGACY_INCLUSIVE_GST_SUB_IDS` env, all new subs exclusive.
- [Xero claim_amount convention vs reconciler](xero-claim-reconcile-conventions.md) — cash-retention inbound MUST use mapItemsDirectly + add retention_amount_with_gst to the header; reconciler at payment-claims.service.ts:262 enforces claim_amount = linesSubtotal+linesGst+retention_with_gst.
- [Xero retention matcher coupling](xero-retention-matcher-coupling.md) — the matcher is pure but leg-repair side effects live inside `handleInvoiceCreateUpdate`; a "skip imported bills" filter on retro-recheck silently drops 15-90 day leg-repair coverage.
- [Xero retention SPLIT & consumption ledger](xero-retention-split-ledger.md) — multi-leg (ex-GST+GST) retention auto-match: record ALL legs in ledger on BOTH create+edit paths; exactCount>1 = ambiguous stop; exclude-id pre-filter scoped by integration.
- [Prod DB access via RAILWAY_DATABASE_URL](prod-db-access.md) — `checkDatabase` only sees the dev DB; for Railway prod run `psql "$RAILWAY_DATABASE_URL"` directly. Check prod data BEFORE guessing about deploys/caches.
- [payment_to_account guard vs trust accounts/deleted duplicates](payment-to-account-guard-and-trust-accounts.md) — supplier-mismatch guard nulls valid trust accts tagged to deleted dup clients; TypeORM findOne(undefined) returns arbitrary row → null journal insert.
- [Railway prod can lag main](railway-deploy-lag.md) — a prod-only error contradicting a clean trace CAN be deploy lag, but verify with deploy-log line markers + prod data first; it can equally be a real bug (e.g. TypeORM findOne(undefined) → arbitrary row). Don't assume lag.
- [Contract PTA resolution divergence](contract-pta-resolution-divergence.md) — user-side dropdown/resolver ANDs PTA lookup with `client_supplier_id`, excluding project-scoped PTAs; Xero smartCreateContract resolves PTA by project_ids only. Keep them in sync.
- [Compliance action-button routing](compliance-action-button-routing.md) — route to where the breach is fixable; a claim is in "Payments to do" only with a payment_details `Unconfirmed%` row, else it's in Claims. New enum values need a migration.
- [Receivable claim → notice/PDF rollback](receivable-claim-notice-rollback.md) — ACCREC claim creation runs notice (+Puppeteer PDF for Paid-delegated) inside the claim txn; any throw → "Notice generation failed" → full rollback → no claim, no record-linked log.
- [xero_sync_logs failure dedup](xero-sync-log-dedup.md) — repeated identical failures bump occurrence_count/last_occurred_at on ONE row, not new rows; ORDER BY created_on hides it. "No second row after re-run" usually means dedup, not no-log.
- [Xero sync-log noise — 3 layers + supersede](xero-sync-log-supersede.md) — dedup-merge vs success-archive vs supersede-on-new-reason; same-field resource key + from_xero + FORBIDDEN denylist are the over-archive guardrails (api_name/sync_type differ across the 265→486 progression, so can't scope by them).
- [Compliance must exclude Deleted contracts](compliance-exclude-deleted-contracts.md) — contract-presence/eligibility finds need `Not('Deleted')`; a deleted contract was deep-linked as EDIT_CONTRACT, opening a deleted contract with wrong supplier.
- [Compliance must exclude Deleted records](compliance-exclude-deleted-payments.md) — every PTA/RTA compliance query over contracts/claims/payments/sub-payments must exclude 'Deleted'; RTA had ZERO filters and was swept. Presence/SUM/late checks all false-fire on deleted+replaced rows.
- [payment_to_account supplier guard](payment-to-account-supplier-guard.md) — validate the to-account against the CLAIM's supplier, not just company; auto-resolve must key off the same canonical supplier id.
- [Journal stale account + ledger fan-out](journal-stale-account-ledger-fanout.md) — journals snapshot payment_to_account; later edits don't regen; ledger groups by txn-account not supplier_id → one stale leg fans onto 2 ledgers.
- [S75 non-paid-reason gate wiring](s75-reason-gate-wiring.md) — error_code lives on the log TEMPLATE; reason form is per-claim; manual (359) must keep project_id + companyId parity with webhook/scheduler; auto-clear re-runs with empty reasons when claims since paid.
- [Xero sync-log template keys](xero-sync-log-template-keys.md) — sync-log message is rendered at read time; `dynamic_values` keys MUST match the template's `{{placeholders}}` exactly or the UI shows raw `{{...}}`.
- [Xero fail-for-input idempotency](xero-fail-for-input-idempotency.md) — handlers re-run on webhook + ~15-min scheduler; any "needs your input" hold must skip if an open same-template/same-external-id log exists, or it duplicates ~96×/day.
- [Client bank details inline-when-required](client-bank-details-optional.md) — optional at contact level; only consumed by ABA on overpayment refunds. Refund-time guard must resolve the SAME account addPayment pays to.
- [Xero invoice handler silent-failure logging](xero-handler-silent-failure-logging.md) — every non-true return in handleInvoiceCreateUpdate MUST persist a Failed sync-log (636/637) or the manual-sync trigger points at nothing.
- [Inbound bill import gate order](inbound-bill-import-gate-order.md) — contact-not-mapped (425) fails BEFORE project/account-code checks; strict isAccountCodeValid rejects overhead-coded bills so smart-create can't fire; size gate hard-stops though PT treats it as a warning; inbound xero_records already stored.
- [Retention BULK vs xero_payments unique constraint](xero-retention-bulk-unique-conflict.md) — 1-transfer→N-retentions can't use `xero_payments` (UNIQUE bank_transfer_id); self-only/ledger-only, advisory-lock re-check on consume, re-sync guard checks confirm flag not just coverage.
- [Xero retention confirm is 1:1 transfer↔payment](xero-retention-confirm-1to1-persistence.md) — SPLIT/BULK accumulate-then-tick needs a confirm-path persistence rework, not just a smarter matcher; trust-movement lane is independent.
- [GraphQL input type mismatches → silent save](graphql-bsb-string.md) — FE sending Number where DTO declares String triggers BAD_USER_INPUT before the resolver runs; `formatError` rewrite hides the inner field. Silent FE catches make it invisible.
- [Compliance checkpoint freshness](compliance-checkpoint-freshness.md) — every successful resync MUST clear `is_stale` (including the no-change branch); entity default is `true` so omitting it makes the refresh worker loop forever and starve the event loop.
- [Xero retention inbound — offsetting retained lines](xero-retention-inbound-prorate.md) — inbound reducer sums `|unitAmount|` per retained line, so `-$X + +$X` adjustments import as `$2X` phantom retention. `adjustItemsWithRetention` prorate denominator is NOT a bug (gross/gross).
- [Compliance cache change-detection](compliance-cache-change-detection.md) — syncCompliancesOfProject hasChanges must compare reference_id; target-only fixes silently no-op otherwise (froze a deleted-contract deep-link).
- [Compliance dashboard jsonb lockstep](compliance-dashboard-jsonb-lockstep.md) — dashboard reads the `compliance_of_projects` jsonb, NOT live tables; the freshness-worker path only writes live tables, so re-project jsonb in refreshProjectComplianceCache or the card lags till the daily cron.
- [Contract form stale draft state](contract-form-stale-draft-state.md) — Edit Contract has 3 competing field sources; the shared never-cleared /api/route-data blob must only defer real hydration for a true contract round-trip, else stale supplier sticks.
- [Xero ABN mirror overflows varchar(11)](xero-abn-mirror-varchar11.md) — Xero taxNumber is space-formatted (14 chars) but PT abn_number is varchar(11); every mirror path must strip+slice(0,11) or the whole contact save throws. Handler must return false + log on failure.
- [Xero inbound contact email NOT NULL](xero-contact-email-notnull.md) — Xero sends emailAddress ""; client_email_id is NOT NULL → coerce `|| ''` (never `|| null`) on every inbound insert; missing email is soft-fail (needs_email). Transient post-deploy 265 self-heals via scheduler.
- [Contact address is one varchar](contact-address-single-column.md) — client_suppliers_details stores the whole address in one column (no suburb/postcode fields); Xero import must FLATTEN all parts, and GooglePlacesInput needs opt-in manual entry whose onChange omits placeDetails.
- [ABA generation shared build path](aba-generation-shared-helper.md) — ABA bytes/upload/save live in one buildAndUploadAbaFile helper shared by generate + regenerate-in-place; APCA comes from the bank account, not sub-payment rows.
- [Xero outbound payment-delete lives in the resolver](xero-payment-delete-outbound-echo.md) — PT→Xero deletePayment fires from the RESOLVER changeStatusOfAPayment, not the service; that split is the anti-echo boundary (inbound calls the service directly). Reconciled mirrors block via internal `block_if_reconciled`.
- [Xero by-design payment-delete refusals](xero-by-design-refusal-as-info.md) — protected-payment delete refusals are ONE idempotent Failed hold (not Info, no occurrence climb); dedup scoped per refusal family in maybeHandleByDesignPaymentDeleteSkip.
- [s76 two date triggers](s76-two-date-triggers.md) — compliance flag breaches only at due_date (pay-in-full exemption); the "missed schedule window" payment-dialog warning keys on received_date+15bd. Never conflate.
- [Reconciling failed outbound Xero payment pushes](xero-payments-link-failed-pushes.md) — if a push failed but the bill was paid in Xero, LINK the existing inbound xero_payments row (set pt_payment_id) by Xero PaymentID, don't insert; invoice_id stores the mirror PK not the Xero id; getInvoice can return a BatchPaymentID.
- [Xero smart-create live contact read](xero-smart-create-live-contact-read.md) — inbound smart-create needs the live getContact (mirror lacks mandatory address/phone/ABN); retry transient empty/failed reads, don't hard-fail 265.
- [Xero outbound bank-account resolution](xero-outbound-bank-account-resolution.md) — one PT account → many Xero rows (Auto DRAFT + Manual ACTIVE); plain findOne picks arbitrary row → "Account could not be found". Use resolvePostableXeroBankAccount at ALL outbound sites.
- [Xero inbound contact entity_type default](xero-inbound-contact-entity-type.md) — entity_type enum has no 'Organisation' (only Business/Sole Trader/Personal/Partnership); inbound contact import must default 'Business' or the insert throws.
- [OpenAI reasoning-model params](openai-reasoning-model-params.md) — GPT-5 family (incl. chat-latest) + o-series reject custom temperature; AND gpt-5.1 reasoning is too slow (>55s) for synchronous proxied calls — default to chat-latest, timeout below the proxy cutoff.
- [OpenAI client guarded init](openai-client-guarded-init.md) — never `new OpenAI()` eagerly in a provider constructor; SDK throws on missing key → crashes Nest boot → Railway healthcheck fails. Guard nullable like existing services.
- [Next.js DB-backed SSR pages](nextjs-db-backed-ssr-pages.md) — admin-editable public pages need `dynamic="force-dynamic"` (+clear `.next` cache) or they serve stale static HTML; SSR sanitization needs isomorphic-dompurify, not browser dompurify.
- [Dashboard status deep-link id keys](dashboard-status-deeplink-id-keys.md) — ai-status-snapshot checkers must emit the uuid PK the view page fetches by, NOT the int business key (report_id/client_supplier_id), or links open wrong/blank records.
- [Xero permanently-unmapped sticky flag pattern](xero-permanently-unmapped-pattern.md) — sticky per-row exclusion across mirror entities; the skip gate must guard EVERY inbound path (webhook + scheduler) or it silently re-imports.
- [Audit-pack Summary.xlsx contents index](audit-pack-summary-sheet.md) — sheet renders blank silently unless the module_name has a getCustomHeaders case AND data rows are flat keyed to those headers; two summary builders share the flaw.
- [Audit-pack ZIP Windows compatibility](audit-pack-zip-windows.md) — archiver's data-descriptor (GP bit3) output makes Windows mis-report "password protected"/0x80004005; write zips w/o data descriptors + keep extracted paths <260.
- [Xero record-mirror failure logs can be suppressed](xero-mirror-failure-log-suppressed.md) — a failed never-imported account/contract/project import writes NO sync-log row (372/394 etc.); debug the resolver's false return + backend "skipped never_imported" line, not the sync-log UI.
- [Trustee withdrawal shortfall scope](trustee-withdrawal-shortfall-scope.md) — Check 7 compliance sums claims PER project; the account-level withdrawal warning aggregates across all `bank_accounts.project_ids`. Divergence is intentional (no project context at withdrawal; only ever over-warns).
- [Bookkeeping CSV import has no dedup](bookkeeping-csv-import-dedup.md) — matched_payment_ids→sub_payment_id, 1 bank line↔1 sub-payment; re-uploading a CSV re-imports already-matched txns (date parse DD/MM vs MM/DD masks it) → phantom duplicate payments. Check transaction_details, not just payment_details.
- [Trust ledger out-of-balance from one-sided bank sweep](trust-ledger-bank-sweep-one-sided.md) — PTA cash bal = bank-leg only (journal_type.beneficiary_type='bank'); inbound matcher records Top-Up return legs of zero-sum NAB sweeps but not the outbound legs → temporary overstatement that self-corrects later.
- [Edit Contract wrong supplier — native select + deleted party](contract-edit-deleted-supplier-select.md) — native <select> shows FIRST option when value matches no option; soft-deleted suppliers/projects are excluded from pickers, so the edit form must re-inject the current value.
- [Activity log impersonation skip](activity-log-impersonation-skip.md) — admin login-as-user actions wrote NO history: insertActivityLog guard dropped is_admin=false+from_user=null rows; fix is `&& !admin_id`, keep is_admin=false (true would hide row from the user view).
